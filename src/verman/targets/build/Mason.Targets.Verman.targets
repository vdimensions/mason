<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <MonoPath Condition="'$(MonoPath)' == '' And Exists('/Library/Frameworks/Mono.framework/Commands/mono')">/Library/Frameworks/Mono.framework/Commands/mono</MonoPath>
    <MonoPath Condition="'$(MonoPath)' == ''">mono</MonoPath>

    <CommandPrefix Condition=" '$(OS)' == 'Windows_NT'"></CommandPrefix>
    <CommandPrefix Condition=" '$(OS)' != 'Windows_NT' ">$(MonoPath) --runtime=v4.0.30319</CommandPrefix>

    <Mason>$(CommandPrefix) &quot;$(MSBuildThisFileDirectory)..\tools\mason.exe&quot;</Mason>
    
    <ProjectFile>$(ProjectName)$(ProjectExt)</ProjectFile>
  </PropertyGroup>
  <Target Name="UpdateVersions" AfterTargets="AfterBuild">
    <Exec Command="$(Mason) verman $(ProjectFile)" WorkingDirectory="$(ProjectDir)" />
  </Target>
</Project>